{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window core permissions; backend process and file export are controlled in Rust.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

